# Example validation data config.
# Schema: list of entries with fields:
# - name: short dataset tag for logging
# - manifest: absolute path to a Lhotse CutSet manifest (jsonl.gz)

- name: dev
  manifest: /abs/path/to/valid_cuts.jsonl.gz