# Test data configuration
# Each entry should have:
#   - name: dataset name for logging
#   - manifest: path to lhotse cuts manifest (jsonl or jsonl.gz)

# Example:
# - name: voxceleb1_test
#   manifest: /path/to/voxceleb1_test_cuts.jsonl.gz
# - name: cremad_test
#   manifest: /path/to/cremad_test_cuts.jsonl.gz

# ----------------------CREMA---------------------------
- name: CREMA-test
  manifest: manifests/CREMA_test.jsonl.gz


# # ----------------------RAVDESS--------------------------- Example: missing age_group label
- name: RAVDESS_valid
  manifest: manifests/RAVDESS_test_pseudo_age.jsonl.gz